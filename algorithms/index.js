const same = require('./modules/same.js');
// const charFreq = require('../modules/charFrequency');

same.same([1, 2, 3], [4, 1, 9]) // true
// same([1, 2, 3], [1, 9]) // true
// same([1, 2, 1], [4, 4, 1])// false
